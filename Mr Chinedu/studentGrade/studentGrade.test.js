const {enterScores, subjectAverages, studentAverages, studentSumFunc, subjectSumFunc, calculatePositions, printTable, printSubjectSummary, printHardestAndEasiestSubject, printOverallHighestAndLowest, printClassSummary} = require('./studentGrade');

// test("enter scores", () => {
//     let scores = (1,[80, 90, 70]); // Arrange
//     let result = enterScores(scores); // Act
//     expect(result).toEqual([{ id: 1, scores: [80, 90, 70] }]) // Assert
// })

// test ("calculate subject averages", () => {
//     let students = [
//         { id: 1, scores: [80, 90, 70] },
//         { id: 2, scores: [85, 95, 75] },
//         { id: 3, scores: [78, 88, 68] }
//     ]; // Arrange
//     let result = subjectAverages(students); // Act
//     expect(result).toEqual([81, 91, 71]) // Assert
// })

// test("ca")